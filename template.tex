% !TeX spellcheck = en-US, de-DE
% !TeX encoding = UTF-8 Unicode
\documentclass[aspectratio=169,9pt]{beamer}

\usetheme[
    %% Language, passed to babel/polyglossia
    english, % or (n)german
    localization=babel, % or polyglossia or none
    biblatexbackend=bibtex8, % or biber or bibtex (7-bit)
    %
    %% Imports
    tikz, % import tikz and commonly used packages
    minted, % import minted and set-up associated environments
    %% Note: Using minted requires the [fragile] option for frames
    % cleveref, % import cleveref and set-up some names
    linkref, % expands cleveref to enable hyperlinks on reference numbers
    %
    %% LuaLaTeX
    % The following options require LuaLaTeX, which may increase compilation times substantially.
    % localization=polyglossia, % This replaces babel
    %
    %% Colors (GU corporate colors are always available)
    ttlabcolors, % TTLab colors
    % materialcolors, % Material default colorscheme colors
    % alphabetcolors, % Alphabet color scheme, REQUIRES CITATION
    % greenarmytagecolors, % Green-Armytage color scheme, REQUIRES CITATION
    %
    %% Layout options
    titlemargin=0.1\paperwidth, % the size of margins left and right of the title
    titleheight=0.4\paperheight, % the hight of the beamercolorbox containing title and subtitle
    titledepth=0\paperheight, % the depth of the beamercolorbox containing title and subtitle
    % centertitle, % center the title and subtitle
    sectionheight=0.5\paperheight, % the height of the section title box section/subsection frame
    frametitlemargin=0.3cm, % the size of the margin left of each frame title and subtitle
    framefootermargin=0.3cm, % the size of the margins left and right in the footline
    logo=images/logo-ttlab.pdf, % can be PDF or any image, is scaled to logowidth
    logowidth=0.4\paperwidth, % you may need to scale your logo to fit the title page
    % centerlogo, % center the logo on the title page
    % nologo, % disable the logo on the title page
    nonavigation, % disable the navigation in the footline
]{goethe}

%%%%%%%%%%%%%%%%%%%%%%%%
% Title, Authors, etc. %
%%%%%%%%%%%%%%%%%%%%%%%%
%% Authors and titles are wrapped in a tblr environment

% Title and sub-title can contain \newline's
\title[Title]{Template Title} % TODO
\subtitle[Subtitle]{Template Subtitle} % TODO

% Authors in "middle" alignment
% Separate multiple authors on the same line with \and
% Use \And to put authors on a separate line
% Use a nested tblr environment for more complex author layouts
\author[Short Authors]{{First Author \and Second Author} \And {Author with a very long Name}} % TODO

% Institute in "header" alignment, top right
\institute[Short Institute]{First Laboratory \And Second Laboratory}

% Date in "footer" alignment, bottom right
\date{\today} % TODO

%%%%%%%%%%%%%%%%%
% Customization %
%%%%%%%%%%%%%%%%%
%% Uncomment and change the following lines to change the footline
% \makeatletter
% \setbeamertemplate{footline separator}{~--~}
% \setbeamertemplate{footline content}{%
%     \let\hyperlink\@secondoftwo%
%     \insertshorttitle\usebeamertemplate{footline separator}\insertshortsubtitle%
% }
% \makeatother

%% Or, change the logo on the title page:
% \makeatletter
% \setbeamertemplate{logo on title page}{%
%    \includegraphics[width=\beamer@goethe@logo@width]{\beamer@goethe@logo} %
% }
% \makeatother

%%%%%%%%%%%%%%%%
% Bibliography %
%%%%%%%%%%%%%%%%
% Note, that we cannot import the whole ACL anthology using biber, as it takes forever to run.
% Instead, we can fall back to the bibtex8 backend by passing biblatexbackend=bibtex8 as an.
% Add the bibliography files using:
\addbibresource{theme/colors.bib}
% \addbibresource{references.bib}

%%%%%%%%%%%%
% Document %
%%%%%%%%%%%%
\begin{document}

\maketitle

\section{Features}\sectionFrame

    \begin{frame}{Lists}{Different Levels and Show Only At/From Slide}
        % \setlength{\labelwidth}{5em}
        \setlength{\leftmargini}{\labelwidth}
        % \setlength{\leftmarginii}{0pt}
        % \setlength{\leftmarginiii}{0pt}]
        \begin{itemize}
            \item First level
            \begin{itemize}
                \item<+(1)-> Second level
                \begin{itemize}
                    \item<+(1)-> Third level
                \end{itemize}
            \end{itemize}
            \item[\adjustbox{max width=\labelwidth}{\faLongArrowAltRight}] ABC
        \end{itemize}
    \end{frame}

    \begin{frame}{Tables with Caption Above}
        Captions for tables are placed \textbf{above} the table, while captions for figures are placed \textbf{below} the figure.
        %
        See \Linkref{tab:1} and \Linkref{fig:1} below.

        \begin{table}
            \centering
            \caption{Caption above.}
            \label{tab:1}
            \begin{tabular}{c|c}
                A & B \\\hline
                C & D \\
            \end{tabular}
        \end{table}

        \begin{figure}
            \centering
            \tcbox{Figures inside \texttt{tcolorbox}}
            \caption{Caption below}
            \label{fig:1}
        \end{figure}
    \end{frame}

    \include{theme/misc/listings}

    \begin{frame}{\texttt{pause}, \texttt{only} \& \texttt{onslide}}
        First
        \pause
        Second
    \end{frame}

    \begin{frame}{\texttt{pause}, \texttt{only} \& \texttt{onslide}}
        \only<2>{
            Second
        }
        First
    \end{frame}

    \begin{frame}{\texttt{pause}, \texttt{only} \& \texttt{onslide}}
        \onslide<2>{
            Second
        }
        First
    \end{frame}

\section{Show TOC, Current Section \& Sub-Sections}\sectionFrame[show subsections]
    \subsection{First Sub-Section}
    \subsection{Second Sub-Section}
    \subsection{Third Sub-Section}

\section{Show TOC, Current Section \& Sub-Sections with Highlight}\sectionFrame[show subsections]
    \subsection{First Sub-Section}\subsectionFrame
    \subsection{Second Sub-Section}\subsectionFrame
    \subsection{Third Sub-Section}\subsectionFrame

\section{Show Current Section \& Sub-Sections}\sectionFrame[only current section]
    \subsection{First Sub-Section}\subsectionFrame[only current section]
    \subsection{Second Sub-Section}\subsectionFrame[only current section]
    \subsection{Third Sub-Section}\subsectionFrame[only current section]

        % Do NOT use allowframebreaks anywhere else! Use counters instead
        \begin{frame}[allowframebreaks]{References}
            \printbibliography
        \end{frame}

\appendix

    % \section{Appendix}
    % \label{sec:appendix}

\end{document}
